import { ReactNode } from 'react';
import { ITextInputProps } from './TextInput.types';
declare const TextInput: ({ bottom, borderColor, borderRadius, center, left, label, labelError, multiline, padding, right, style, styleLabel, top, width, ...props }: ITextInputProps) => ReactNode;
export default TextInput;
