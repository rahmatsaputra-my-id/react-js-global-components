import { ReactNode } from 'react';
import { IImageProps } from './Image.types';
declare const Image: ({ bottom, center, height, left, resizeMode, right, style, top, width, ...props }: IImageProps) => ReactNode;
export default Image;
