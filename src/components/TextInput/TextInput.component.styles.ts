import { Colors } from '../../constants/Colors';

export const styles = {
  label: {
    marginBottom: 4,
    fontSize: 14,
  },
  labelError: {
    marginTop: 2,
    fontSize: 14,
    color: Colors.red,
    maxWidth: 300,
  },
  textArea: {
    paddingTop: 8,
    paddingLeft: 8,
    paddingRight: 8,
    paddingBottom: 16,
  },
  textInput: {
    padding: 8,
  },
};
